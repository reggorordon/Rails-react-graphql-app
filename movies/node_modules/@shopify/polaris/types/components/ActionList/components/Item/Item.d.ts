/// <reference types="react" />
import { ActionListItemDescriptor } from '../../../../types';
export declare type Props = ActionListItemDescriptor;
export default function Item({ badge, content, url, onAction, icon, image, disabled, external, destructive, ellipsis, }: Props): JSX.Element;
